TODO:
 - set an active player character. The current version simply dumps you into character creation, something like
   MikeysToolbox should work for the interim.
 - handle the static_buildables table (just ignored currently)
 - test whether inventories are intact, both of players as well as of placeables
 - handle overlapping buildings?
 - build a UI
